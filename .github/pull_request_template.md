**Closes #**

**What changed**

**Self-review checklist**
- [ ] Tests pass locally (`make test`) and lint is clean (`make lint`)
- [ ] No dataset files, model binaries or secrets added
- [ ] Vectorisation still happens inside the pipeline, not before the split
- [ ] If results changed, `results/results.csv` and the figures were regenerated
- [ ] Docs updated if behaviour or reproduction steps changed
